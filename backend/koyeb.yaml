name: ai-interview-backend
services:
  - name: api
    type: web
    instance_type: free
    regions:
      - fra
    ports:
      - port: 5000
        protocol: http
    env:
      - name: NODE_ENV
        value: production
    build:
      type: buildpack
    run:
      command: npm start
